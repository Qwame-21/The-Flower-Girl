// TODO: Design RequestsPage — custom request briefs, quoting, approve and convert to order
export default function RequestsPage({ activeTab, filteredRequests, setSelectedItem }) {
  return (
    <div className="admin-page-stub">
      <h2>Custom Requests</h2>
      <p>Request briefs, quoting and order conversion will be built here.</p>
    </div>
  );
}
