// TODO: Design SettingsPage — admin name, notifications, store config, payment keys
export default function SettingsPage({ adminData, setActiveTabs }) {
  return (
    <div className="admin-page-stub">
      <h2>Settings</h2>
      <p>Store configuration, admin profile and notification preferences will be built here.</p>
    </div>
  );
}
